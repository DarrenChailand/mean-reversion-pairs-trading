# Contributing

1. Create a branch from `main`.
2. Install development tools with `pip install -e ".[dev]"`.
3. Run `ruff check .` and `pytest` before opening a pull request.
4. Keep changes small and add tests for research logic.
5. Explain any assumption that can change backtest results.

